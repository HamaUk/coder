# Process definition for hosts that read a Procfile (Railway, Heroku-style
# runners, some Docker-less platforms).
#
# The server reads HOST and PORT from the environment and requires HAMA_TOKEN
# whenever HOST is not loopback.
web: node server.js
